
1. Prerequisite installs:
Please open a new terminal and run the following commands before running any of the notebooks

pip install textblob
python -m textblob.download_corpora
pip install gensim
pip install pyLDAvis
pip install wordcloud

2. Download the stopwords corpus from nltk
Type python so you access to the IDLE
Type import nltk
Type nltk.download('stopwords')
